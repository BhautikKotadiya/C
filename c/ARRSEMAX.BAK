// find second max;

#include<stdio.h>
#include<conio.h>
void main()
{
	int a[5]={1,2,3,4,5};
	int i,smax=0,max=0;
	clrscr();
	for(i=0;i<5;i++)
	{
		if(max<a[i])
		{
			smax=max;
			max=a[i];
		}
		else if(smax<a[i])
		{
			smax=a[i];
		}
	}
	printf("your ans is:- %d",smax);
	getch();
}